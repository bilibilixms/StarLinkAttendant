# 开发环境：混合模式（VITE_USE_MOCK=true + 会员端真实接口穿透）
# - 会员端登录/充值/积分、自助上机（开机/当前会话/下机结算/临时下机）接口走真实后端
#   http://localhost:8080/api/member/**，由 api/request.ts 的 isMemberEndpoint() 识别后穿透 mock 层。
# - 其他业务（订单/活动等）后端尚未实现，继续走本地 mock 保证小程序可跑通。
VITE_USE_MOCK=true
VITE_API_BASE_URL=http://localhost:8080
