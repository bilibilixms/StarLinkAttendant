# 小程序用户端环境变量（所有环境共享，可被 .env.development / .env.production 覆盖）

# 接口地址前缀
# - H5 开发：留空走同源，vite.config.ts 的 proxy 会把 /api 转发到 http://localhost:8080
# - 微信小程序：不能用相对路径，必须写完整域名；开发期间直连本地后端 8080
# - 部署到测试/正式环境时换成对应域名（需在微信后台配置 request 合法域名）
VITE_API_BASE_URL=

VITE_APP_TITLE=星络灵侍馆
