<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starlink.member.mapper.MemberMapper">

    <select id="selectMemberPage" resultType="com.starlink.member.dto.resp.MemberResponse">
        SELECT
            m.id, m.member_no, m.real_name, m.gender, m.phone, m.birthday,
            m.level_id, ml.level_name, m.total_points, m.available_points,
            m.growth_value,
            m.total_recharge, m.balance, m.total_consumption, m.last_login_time,
            m.last_online_time, m.total_online_hours, m.register_source,
            m.status, m.blacklist_reason, m.tag, m.created_at, m.updated_at
        FROM member m
        LEFT JOIN member_level ml ON m.level_id = ml.id
        WHERE m.deleted_at IS NULL
        <if test="memberNo != null and memberNo != ''">
            AND m.member_no LIKE CONCAT('%', #{memberNo}, '%')
        </if>
        <if test="realName != null and realName != ''">
            AND m.real_name LIKE CONCAT('%', #{realName}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND m.phone LIKE CONCAT('%', #{phone}, '%')
        </if>
        <if test="status != null">
            AND m.status = #{status}
        </if>
        <if test="levelId != null">
            AND m.level_id = #{levelId}
        </if>
        ORDER BY m.created_at DESC
    </select>

    <select id="selectByPhone" resultType="com.starlink.member.entity.Member">
        SELECT * FROM member WHERE phone = #{phone} AND deleted_at IS NULL
    </select>

    <!-- 含已注销记录：用于注册时检测手机号是否被软删除记录占用 -->
    <select id="selectByPhoneIncludeDeleted" resultType="com.starlink.member.entity.Member">
        SELECT * FROM member WHERE phone = #{phone} ORDER BY deleted_at IS NULL DESC, id DESC LIMIT 1
    </select>

    <!--
        复活已注销会员。必须用手写 SQL：实体 deletedAt 标注 @TableLogic，
        MyBatis-Plus 内置 update 会自动追加 deleted_at IS NULL 条件，导致对已删除行的更新匹配 0 行。
    -->
    <update id="reviveDeletedMember">
        UPDATE member SET
            member_no = #{memberNo},
            password_hash = #{passwordHash},
            real_name = #{realName},
            gender = #{gender},
            id_card = #{idCard},
            birthday = #{birthday},
            level_id = #{levelId},
            total_points = 0,
            available_points = 0,
            growth_value = 0,
            total_recharge = 0,
            balance = 0,
            total_consumption = 0,
            register_source = 1,
            status = 1,
            deleted_at = NULL,
            version = version + 1
        WHERE id = #{id}
    </update>

    <select id="selectByMemberNo" resultType="com.starlink.member.entity.Member">
        SELECT * FROM member WHERE member_no = #{memberNo} AND deleted_at IS NULL
    </select>

    <select id="selectMemberDetail" resultType="com.starlink.member.dto.resp.MemberResponse">
        SELECT
            m.id, m.member_no, m.real_name, m.gender, m.phone, m.birthday,
            m.level_id, ml.level_name, m.total_points, m.available_points,
            m.growth_value,
            m.total_recharge, m.balance, m.total_consumption, m.last_login_time,
            m.last_online_time, m.total_online_hours, m.register_source,
            m.status, m.blacklist_reason, m.tag, m.created_at, m.updated_at
        FROM member m
        LEFT JOIN member_level ml ON m.level_id = ml.id
        WHERE m.id = #{id} AND m.deleted_at IS NULL
    </select>

    <select id="selectBlacklist" resultType="com.starlink.member.dto.resp.BlacklistResponse">
        SELECT id, member_no, real_name, phone, blacklist_reason, created_at
        FROM member WHERE status = 3 AND deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

</mapper>