/**
 * 会员聚合接口占位。
 *
 * 历史接口（getMemberSummary / signIn / verifyIdentity）已删除：
 * - 后端 starlink-module-member 暂未提供 /api/member/summary 等聚合接口
 * - 签到、实名认证等业务后端尚未实现，相关页面已切换为 toast 占位
 *
 * 后续后端补齐后，在此文件内按需补回对应方法。
 */
export {}
