<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starlink.system.mapper.PermissionMapper">

    <select id="selectByRoleId" resultType="com.starlink.system.entity.Permission">
        SELECT p.* FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId} AND p.deleted_at IS NULL
    </select>

    <select id="selectByRoleIds" resultType="com.starlink.system.entity.Permission">
        <if test="roleIds != null and roleIds.size() > 0">
        SELECT DISTINCT p.* FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND p.deleted_at IS NULL
        </if>
    </select>

    <select id="selectTreeByParentId" resultType="com.starlink.system.entity.Permission">
        SELECT * FROM permission WHERE parent_id = #{parentId} AND deleted_at IS NULL
    </select>

</mapper>