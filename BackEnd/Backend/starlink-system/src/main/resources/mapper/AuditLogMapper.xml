<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starlink.system.mapper.AuditLogMapper">

    <select id="selectLogPage" resultType="com.starlink.system.dto.resp.LogResponse">
        SELECT
            id, operator_id, operator_name, biz_type, biz_id,
            action, detail, ip_address, user_agent, created_at
        FROM audit_log
        WHERE deleted_at IS NULL
        <if test="operatorName != null and operatorName != ''">
            AND operator_name LIKE CONCAT('%', #{operatorName}, '%')
        </if>
        <if test="bizType != null and bizType != ''">
            AND biz_type = #{bizType}
        </if>
        <if test="action != null and action != ''">
            AND action = #{action}
        </if>
        <if test="startTime != null and startTime != ''">
            AND created_at &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND created_at &lt;= #{endTime}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>