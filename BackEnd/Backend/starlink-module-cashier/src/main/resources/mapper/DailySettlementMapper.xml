<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starlink.cashier.mapper.DailySettlementMapper">

    <select id="selectByDate" resultType="com.starlink.cashier.entity.DailySettlement">
        SELECT *
        FROM daily_settlement
        WHERE settle_date = #{settleDate}
          AND deleted_at IS NULL
        LIMIT 1
    </select>

    <select id="selectSettlementPage" resultType="com.starlink.cashier.dto.response.SettlementResponse">
        SELECT *
        FROM daily_settlement
        WHERE deleted_at IS NULL
        <if test="startTime != null and startTime != ''">
            AND settle_date &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND settle_date &lt;= #{endTime}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY settle_date DESC
    </select>

</mapper>
