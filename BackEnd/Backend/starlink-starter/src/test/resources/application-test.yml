# =============================================
# 星络灵侍馆 — 测试专用配置（P0-3）
# =============================================
# 本文件位于 src/test/resources，**只存在于测试类路径**，
# 不会被打进任何生产产物（jar / war），因此不构成生产默认密钥。
#
# 作用：让自动化测试无需依赖外部环境变量即可启动（应用启动时
# JwtSecretValidator 会强制校验 JWT 密钥，缺失即启动失败）。
#
# 启用方式：测试类使用 @ActiveProfiles({"dev", "test"})。
# 生产/开发环境仍然必须通过环境变量 JWT_SECRET 提供各自的真实密钥。
jwt:
  secret: starlink-attendant-test-only-secret-4f8c1a6e2b9d7f30a5c8e1b4
